# yaml-language-server: $schema=https://spec.openapis.org/oas/3.1/schema/2021-05-20
---
# for the intellisense
openapi: "3.1.0"
info: { title: "", version: "" }

components:
  schemas:
    BadgeCategory:
      title: BadgeCategory
      type: "string"
      enum:
        - "achievement"
        - "season"
        - "event"
        - "contest"
      description: |
        뱃지 종류입니다.
